logging:
  level:
    org:
      springframework:
        web: DEBUG
    com:
      isakov:
        encore: DEBUG
spring:
  flyway:
    enabled: true

  jpa:
    show-sql: true
#    hibernate:
#      ddl-auto: validate
